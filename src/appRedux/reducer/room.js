import * as types from "../../constants/ActionType";

let initialState = [];

const cart = (state = initialState, action) => {
  switch (action.type) {
    default:
      return [...state];
  }
};

export default cart;
